name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      # Setup key
      - run: set -eu
      - run: mkdir "$HOME/.ssh"
      - run: echo "$SSH_PRIVATE_KEY"
      - run: echo "$SSH_PRIVATE_KEY" > "$HOME/.ssh/id_ed25519"
      - run: chmod 600 "$HOME/.ssh/id_ed25519"
      # Build
      - run: npm ci
      - run: npm run build
      # Deploy
      - run: cd dist && rsync -e "ssh -p 22 -i $HOME/.ssh/id_ed25519 -o StrictHostKeyChecking=no" --archive --compress --delete . lllkojlhuk_ssh@lllkojlhuk.beget.tech:/react-developer-portfolio/public_html